{
  "name": "web-notifier",
  "version": "1.0.2",
  "description": "Easily send push notifications to your users through a service worker!",
  "main": "./src/index.js",
  "scripts": {
    "start": "tsnd --respawn --exit-child --watch src ./test",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "brandon-pereira",
  "license": "ISC",
  "dependencies": {
    "web-push": "^3.4.5"
  },
  "devDependencies": {
    "@tsconfig/node16": "^1.0.2",
    "@types/node": "^17.0.0",
    "@types/web-push": "^3.3.2",
    "dotenv": "^10.0.0",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.5.2"
  }
}
