{
  "name": "web-notifier",
  "version": "2.0.2",
  "description": "Easily send push notifications to your users through a service worker!",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": "github:brandon-pereira/web-notifier",
  "scripts": {
    "start": "tsnd --respawn --exit-child --watch src ./test",
    "test": "tsc --noemit",
    "build": "tsc",
    "prepublish": "npm test && npm run build"
  },
  "author": "brandon-pereira",
  "license": "ISC",
  "dependencies": {
    "web-push": "^3.4.5"
  },
  "peerDependencies": {
    "mongoose": "^6.1.5"
  },
  "devDependencies": {
    "@tsconfig/node16": "^1.0.2",
    "@types/node": "^17.0.0",
    "@types/web-push": "^3.3.2",
    "dotenv": "^10.0.0",
    "mongoose": "^6.1.5",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.5.2"
  }
}
